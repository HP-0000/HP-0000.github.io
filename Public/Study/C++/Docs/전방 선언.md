---
---
컴파일러는 `#include` 를 만나면 해당 헤더를 그대로 복사 붙여 넣는다.
또한 cpp단위로 컴파일이 진행된다.

같은 헤더 파일을 여러 헤더에서 `#include` 를 할 때 필요 없는 내용들도 모두 복사가 된다.
따라서 헤더에서는 주로 전방 선언을 사용한다.

전방 선언을 한 클래스를 헤더에서 사용할 경우, 
값이나 상속에는 사용할 수 없고 포인터나 참조로 사용해야한다.
